// import '../styles/style.css';
import { useDispatch, useSelector } from 'react-redux'
import { useEffect } from 'react';

const ShowBlog = ({id}) => {
  const dispatch = useDispatch()
  useEffect(()=>{},[]);
  return (
    <>
    <div>welcome to show blog</div>
    </>
  );
};

export default ShowBlog;
